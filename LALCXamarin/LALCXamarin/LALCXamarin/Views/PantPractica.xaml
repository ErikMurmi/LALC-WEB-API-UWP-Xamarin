<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="LALCXamarin.Views.PantPractica"
             Title="Práctica">
    <ContentPage.Content>
        <StackLayout Margin="25,80,25,0">
            <CarouselView x:Name="CarruselConceptos"
                      PositionChanged="CarruselConceptos_PositionChanged"
                          VerticalOptions="Center"
                          HeightRequest="300">
                <CarouselView.ItemTemplate>
                    <DataTemplate>
                        <Frame BorderColor="#6e625b"
                           CornerRadius="8"
                           HorizontalOptions="Center"
                           Margin="20"
                           VerticalOptions="CenterAndExpand"
                               >
                            <StackLayout>
                                <Label x:Name="TituloConce"
                                   HorizontalOptions="Center"
                                   Text="{Binding Titulo}}"
                                   FontAttributes="Bold"
                                   FontSize="60"/>
                                <Label x:Name="Concepto" 
                                        HorizontalTextAlignment="Center"
                                        Text="{Binding Definicion}"
                                        FontSize="Small"
                                       MaxLines="10"
                                        LineBreakMode="TailTruncation"/>
                            </StackLayout>
                        </Frame>
                    </DataTemplate>
                </CarouselView.ItemTemplate>
            </CarouselView>
            <Button x:Name="GuardarPractica" Text="Guardar" Clicked="GuardarPractica_Clicked" Margin="50,30,50,30"></Button>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>